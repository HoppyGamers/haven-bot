// Custom command handlers go here
// Each file should export a function that receives the bot instance

module.exports = function registerCustomCommands(bot) {
  // Example: Register a custom command
  // bot.registerCommand('mycommand', 'Description of my command');
  
  // Example: Handle a custom command
  // bot.on('command', async (data) => {
  //   if (data.command === 'mycommand') {
  //     await bot.sendMessage('Custom command handled!');
  //   }
  // });
  
  console.log('Custom commands loaded');
};
